

class DBSetup {

    constructor (host, port, user, password, scheme) {
        this.host = host;
        this.port = port;
        this.user = user; 
        this.password = password; 
        this.scheme = scheme;
    }

} 

export default DBSetup;